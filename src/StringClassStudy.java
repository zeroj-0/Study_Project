public class StringClassStudy {
    public static void main(String[] args) {
        /**
         * String클래스는 자기자신이 변하지 않는 불변 클래스
         * StringBuffer는 변하는 클래스
         *
         * StringBuffer 객체 생성
         * StringBuffer sb = new StringBuffer();
         * 추가 메소드 => append 사용!
         * StringBuffer가 가지는 메소드들은 대부분 자기자신, this를 반환
         *      자기자신의 메소드를 호출하여 자기 자신의 값을 바꿔나가는 것을 메소드체이닝이라고 함
         *
         * String에서 정수형 변수를 문자형 변수처럼 바뀌어 출력하는가 ? 덧셈 연산자를 진행할때 연산자중 한쪽이 String형이면 나머지 쪽을 String 형태로 변환 한 다음 두 String형 문자열을 결합하는 방
         *
         */
    }
}
